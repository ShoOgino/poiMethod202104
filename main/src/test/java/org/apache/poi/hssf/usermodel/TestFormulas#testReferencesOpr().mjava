    @Test
    void testReferencesOpr() throws IOException {
        String[] operation = new String[] {
                            "+", "-", "*", "/", "^", "&"
                           };
        for (final String op : operation) {
            operationRefTest(op);
        }
    }

