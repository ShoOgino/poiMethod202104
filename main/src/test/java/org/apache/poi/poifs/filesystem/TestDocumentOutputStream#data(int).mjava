    private static byte[] data(int len) {
        byte[] input = new byte[len];
        for (int i = 0; i < len; i++) {
            input[i] = (byte)('0' + (i%10));
        }
        return input;
    }

