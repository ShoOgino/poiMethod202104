    public PageSettingsBlock(RecordStream rs) {
        _plsRecords = new ArrayList<>();
        while(true) {
            if (!readARecord(rs)) {
                break;
            }
        }
    }

