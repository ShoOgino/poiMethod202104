    public void setLayout(XDDFLayout layout) {
        if (layout == null) {
            if (legend.isSetLayout()) {
                legend.unsetLayout();
            }
        } else {
            legend.setLayout(layout.getXmlObject());
        }
    }

