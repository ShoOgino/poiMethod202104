    public Double getLeftInset() {
        if (props.isSetLIns()) {
            return Units.toPoints(POIXMLUnits.parseLength(props.xgetLIns()));
        } else {
            return null;
        }
    }

