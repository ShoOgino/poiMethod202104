    public void setLineCap(LineCap cap) {
        if (cap == null) {
            if (props.isSetCap()) {
                props.unsetCap();
            }
        } else {
            props.setCap(cap.underlying);
        }
    }

