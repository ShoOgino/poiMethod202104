    public ErrorDirection getErrorDirection() {
        if (bars.isSetErrDir()) {
            return ErrorDirection.valueOf(bars.getErrDir().getVal());
        } else {
            return null;
        }
    }

