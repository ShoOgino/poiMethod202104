    public Double getPosition() {
        if (stop.isSetPos()) {
            return Units.toPoints(POIXMLUnits.parseLength(stop.xgetPos()));
        } else {
            return null;
        }
    }

