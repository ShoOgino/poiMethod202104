    public void setBackgroundColor(XDDFColor color) {
        if (color == null) {
            if (props.isSetBgClr()) {
                props.unsetBgClr();
            }
        } else {
            props.setBgClr(color.getColorContainer());
        }
    }

