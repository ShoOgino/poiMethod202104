    public void setAttrib(float attrib)
        throws MyException
    {
//        if (attrib < 0.001f)
//           throw new MyException("too small: " + attrib);
        this.attrib = attrib;
    }

