    public static String gobbleUpReader(Reader in)
            throws Exception
    {
        StringBuffer buf = new StringBuffer();
        int c;

        while (-1 != (c = in.read()))
        {
            buf.append(c);
        }

        return buf.toString();
    }

