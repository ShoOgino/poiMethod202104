    public void testPushNTimes(){
	int nCount=100;
	for (int i=0;i<nCount;i++)
	    m_xc.push();
	boolean result=true;
	for (int i=0;i<nCount;i++)
	    result&=m_xc.pop();
	assertEquals(true,result);
	assertEquals(false,m_xc.pop());
    }

