    public void testAddAfterDispose()
    {
        m_xc.dispose();
        boolean error = false;
        try {
            m_xc.addToSelection();
        } catch (Throwable e) {
            error = true;
        }
        assertEquals(true, error);

    }

