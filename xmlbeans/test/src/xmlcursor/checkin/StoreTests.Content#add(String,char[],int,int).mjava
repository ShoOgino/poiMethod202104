        private void add ( String s, char[] buf, int off, int cch )
        {
            _sb.append( s );
            if (buf != null)
            _sb.append( buf, off, cch );
            _sb.append( "\n" );
        }

