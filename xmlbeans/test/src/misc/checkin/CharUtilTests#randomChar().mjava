    private char randomChar ( )
    {
        int n = rnd( 27 );
        
        switch ( n )
        {
            case 0  : return ' ';
            default : return (char)( 'a' + n - 1 );
        }
    }

