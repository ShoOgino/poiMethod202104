    public void insertAttr() {
        XmlCursor c = getCursor();

        while (!c.isEnddoc() && !c.isContainer())
            c.toNextToken();

        if (c.isEnddoc())
            return;

        c.toNextToken();

        c.insertAttribute(getQName());
    }

