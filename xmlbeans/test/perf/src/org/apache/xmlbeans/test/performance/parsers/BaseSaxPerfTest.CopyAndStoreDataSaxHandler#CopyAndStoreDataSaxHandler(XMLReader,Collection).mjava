        CopyAndStoreDataSaxHandler(XMLReader r, Collection c)
        {
            super(r);
            if (c==null)
                throw new IllegalArgumentException();
            _store = c;
        }

