    public static void main(String[] args)
    {
        if (args.length == 0) {
            printUsage();
            System.exit(0);
            return;
        }

        for (int i = 0; i < args.length; i++)
        {
            dump(new File(args[i]), true);
        }
    }

