        protected void emitComment ( SaveCur c )
        {
            assert c.isComment();

            emit( "<!--" );

            c.push();
            c.next();

            emitCommentText( c );

            c.pop();

            emit( "-->" );
        }

