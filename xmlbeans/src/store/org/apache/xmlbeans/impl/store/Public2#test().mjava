    public static void test ( ) throws Exception
    {
        Xobj x = (Xobj) Public2.parse( "<a>XY</a>" );
        
        Locale l = x._locale;

        l.enter();

        try
        {
            Cur c = x.tempCur();

            c.next();
            
            Cur c2 = c.tempCur();
            c2.next();
            
            Cur c3 = c2.tempCur();
            c3.nextChars( 1 );
            
            Cur c4 = c3.tempCur();
            c4.nextChars( 1 );

            c.dump();
            
            c.moveNodeContents( c, true );
            
            c.dump();
        }
        catch ( Throwable e )
        {
            e.printStackTrace();
        }
        finally
        {
            l.exit();
        }
    }

