    /**
     * Adds a name to the representing NameSet
     * @param name
     */
    public void add(String name)
    {
        if (_isFinite )
            _finiteSet.add(name);
        else
            _finiteSet.remove(name);
    }

