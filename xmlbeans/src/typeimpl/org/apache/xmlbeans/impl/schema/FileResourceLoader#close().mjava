    public void close()
    {
        if (_zipfile != null)
        {
            try
            {
                _zipfile.close();
            }
            catch (IOException e)
            {
                // oh well.
            }
            _zipfile = null;
        }
    }

