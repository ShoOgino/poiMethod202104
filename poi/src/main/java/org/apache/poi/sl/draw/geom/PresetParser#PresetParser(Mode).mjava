    PresetParser(Mode mode) {
        this.mode = mode;
        if (mode == Mode.SHAPE) {
            customGeometry = new CustomGeometry();
            geom.put("custom", customGeometry);
        }
    }

