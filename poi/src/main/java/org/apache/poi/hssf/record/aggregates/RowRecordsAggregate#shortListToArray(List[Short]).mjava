    private static short[] shortListToArray(List<Short> list) {
        final short[] arr = new short[list.size()];
        int idx = 0;
        for (Short s : list) {
            arr[idx++] = s;
        }
        return arr;
    }

