	public CatLabRecord(CatLabRecord other) {
		super(other);
		rt = other.rt;
		grbitFrt = other.grbitFrt;
		wOffset = other.wOffset;
		at = other.at;
		grbit = other.grbit;
		unused = other.unused;
	}

