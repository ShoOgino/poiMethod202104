    @Override
    public String toString()
    {
        if ( isEmpty() )
            return "[SHD] EMPTY";

        return "[SHD] (cvFore: " + getCvFore() + "; cvBack: " + getCvBack()
                + "; iPat: " + getIpat() + ")";
    }

