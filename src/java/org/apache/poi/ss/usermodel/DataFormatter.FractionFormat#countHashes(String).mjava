       private int countHashes(String format) {
    	   int count = 0;
    	   for (int i=format.length()-1; i >= 0; i--) {
    		   if (format.charAt(i) == '#') {
    			   count++;
    		   }
    	   }
    	   return count;
       }

