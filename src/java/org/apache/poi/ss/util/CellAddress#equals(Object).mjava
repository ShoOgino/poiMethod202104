    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if(!(o instanceof CellAddress)) {
            return false;
        }
        
        CellAddress cr = (CellAddress) o;
        return _row == cr._row
                && _col == cr._col
        ;
    }

