        /**
         * is this node red?
         *
         * @param index _KEY or _VALUE
         *
         * @return true if non-black
         */
        public boolean isRed(int index)
        {
            return !_black[ index ];
        }

