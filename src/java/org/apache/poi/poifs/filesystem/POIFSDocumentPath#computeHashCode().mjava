    private int computeHashCode() {
        int code = 0;
        for (int j = 0; j < components.length; j++)
        {
            code += components[ j ].hashCode();
        }
        return code;
    }

