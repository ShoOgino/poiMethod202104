    String toJavaString()
    {
        if ( _value.length == 0 )
            return null;

        return StringUtil.getFromUnicodeLE( _value, 0,
                ( _value.length - 2 ) >> 1 );
    }

