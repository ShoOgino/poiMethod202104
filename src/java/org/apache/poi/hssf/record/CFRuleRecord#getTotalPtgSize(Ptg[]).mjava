	private short getTotalPtgSize(Ptg[] ptgs)
	{
		if( ptgs == null) {
			return 0;
		}
		short  retval = 0;
		for (int i = 0; i < ptgs.length; i++)
		{
			retval += ptgs[i].getSize();
		}
		return retval;
	}

