    /**
     * @param in unused (since this record has no data)
     */
    public WriteProtectRecord(RecordInputStream in)
    {
        if (in.remaining() == 2) {
            in.readShort();
        }
    }

