    public int getRecordSize()
    {
        int retval = 4;

        if (getHeaderLength() != 0)
        {
            retval++;
        }
        retval += getHeaderLength();
        return retval;
    }

