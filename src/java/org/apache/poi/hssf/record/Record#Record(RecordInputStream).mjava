    /**
     * Constructor Record
     *
     * @param id record id
     * @param size record size
     * @param data raw data
     */

    public Record(RecordInputStream in)
    {
        validateSid(in.getSid());
        fillFields(in);
    }

