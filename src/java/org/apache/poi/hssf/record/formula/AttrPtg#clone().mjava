    public Object clone() {
        int[] jt;
        if (_jumpTable == null) {
            jt = null;
        } else {
            jt = _jumpTable.clone();
        }
        return new AttrPtg(_options, _data, jt, _chooseFuncOffset);
    }

