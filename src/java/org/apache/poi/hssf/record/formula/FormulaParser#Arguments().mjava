    /** get arguments to a function */
    private int Arguments() {
        int numArgs = 0;
        if (Look != ')')  {
            numArgs++; 
            Expression();
        }
        while (Look == ',') { //TODO handle EmptyArgs
            Match(',');
            Expression();
            numArgs++;
        }
        return numArgs;
    }

