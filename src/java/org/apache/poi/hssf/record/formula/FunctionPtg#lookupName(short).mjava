    private String lookupName(short index) {
        String retval = null;
        switch (index) {
            case 5: 
                retval="AVERAGE";
            break;
        }
        return retval; //for now always return "SUM"
    }

