  /** This method will read a byte from the current record*/
  public int read() {
    checkRecordPosition();

    byte result = data[recordOffset];
    recordOffset += 1;
    pos += 1;
    return result;    
  }

