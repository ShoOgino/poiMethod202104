    public int getPhysicalNumberOfCellsInRow(int row) {
        int count = -1;
        int col = -1;

        while (col > 0 || count == -1) {
            col = findNextPopulatedCell(row,col);
            count++;
        }
        return count;
    }

