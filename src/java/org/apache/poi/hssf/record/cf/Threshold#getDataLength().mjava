    public int getDataLength() {
        int len = 1;
        if (formula != null) {
            len += formula.getEncodedSize();
        } else {
            len += 2;
        }
        if (value != null) {
            len += 8;
        }
        len += 5;
        return len;
    }

