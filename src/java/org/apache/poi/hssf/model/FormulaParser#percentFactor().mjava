    private void percentFactor() {
        tokens.add(parseSimpleFactor());
        while(true) {
            SkipWhite();
            if(look != '%') {
                return;
            }
            Match('%');
            tokens.add(new PercentPtg());
        }
    }

