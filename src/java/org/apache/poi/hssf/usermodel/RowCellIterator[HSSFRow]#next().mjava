    public Object next() {
        HSSFCell retval = null;
        if (cells.hasNext()) {
            retval = new HSSFCell(book, sheet, row, ((CellValueRecordInterface)cells.next()));
        }
        return retval;
    }

