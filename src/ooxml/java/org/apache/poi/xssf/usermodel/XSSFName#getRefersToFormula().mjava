    public String getRefersToFormula() {
        String result = ctName.getStringValue();
        if (result == null || result.length() < 1) {
            return null;
        }
        return result;
    }

