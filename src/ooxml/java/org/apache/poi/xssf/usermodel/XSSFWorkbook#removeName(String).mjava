    /**
     * removes the name
     *
     * @param name range
     *             name index
     */
    public void removeName(String name) {
        //TODO
        //int index=getNameIndex(name);
        //removeName(index);
    }

