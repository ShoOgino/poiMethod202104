    private boolean sameTheme(XSSFColor other) {
        if (isThemed() == other.isThemed()) {
            if (isThemed()) {
                return getTheme() == other.getTheme();
            }
            return true;
        }
        return false;
    }

