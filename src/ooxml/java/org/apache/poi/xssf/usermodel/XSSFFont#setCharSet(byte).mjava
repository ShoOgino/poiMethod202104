    /**
     * set character-set to use.
     *
     * @param charset - charset
     * @see FontCharset
     */
    public void setCharSet(byte charset) {
       int cs = (int)charset;
       if(cs < 0) {
          cs += 256;
       }
       setCharSet(cs);
    }

