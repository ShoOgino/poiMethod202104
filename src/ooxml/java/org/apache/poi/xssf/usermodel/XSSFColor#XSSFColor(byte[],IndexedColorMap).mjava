    /**
     *
     * @param rgb bytes
     * @param colorMap 
     */
    public XSSFColor(byte[] rgb, IndexedColorMap colorMap) {
        this(CTColor.Factory.newInstance(), colorMap);
        ctColor.setRgb(rgb);
    }

