    private boolean sameTint(XSSFColor other) {
        if (hasTint() == other.hasTint()) {
            if (hasTint()) {
                return getTint() == other.getTint();
            }
            return true;
        }
        return false;
    }

