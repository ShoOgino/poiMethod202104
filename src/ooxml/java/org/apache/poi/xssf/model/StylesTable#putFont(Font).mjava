	public long putFont(Font font) {
        int idx = fonts.indexOf(font);
        if (idx != -1) {
            return idx;
        }
        fonts.add((XSSFFont)font);
        return fonts.size() - 1;
	}

