    public static XSSFBRecordType lookup(int id) {
        XSSFBRecordType type = TYPE_MAP.get(id);
        if (type == null) {
            return Unimplemented;
        }
        return type;
    }

