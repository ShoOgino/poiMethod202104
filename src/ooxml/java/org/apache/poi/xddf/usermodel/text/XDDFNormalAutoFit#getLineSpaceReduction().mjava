    @Override
    public int getLineSpaceReduction() {
        if (autofit.isSetLnSpcReduction()) {
            return autofit.getLnSpcReduction();
        } else {
            return 0;
        }
    }

