    public void setScaled(Boolean scaled) {
        if (scaled == null) {
            if (props.isSetScaled()) {
                props.unsetScaled();
            }
        } else {
            props.setScaled(scaled);
        }
    }

