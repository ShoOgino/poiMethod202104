    public Double getBottomInset() {
        if (props.isSetBIns()) {
            return Units.toPoints(POIXMLUnits.parseLength(props.xgetBIns()));
        } else {
            return null;
        }
    }

