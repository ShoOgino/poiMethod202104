    public void setAngle(Integer angle) {
        if (angle == null) {
            if (props.isSetAng()) {
                props.unsetAng();
            }
        } else {
            props.setAng(angle);
        }
    }

