    @Internal
    public void setBlip(CTBlip blip) {
        if (blip == null) {
            if (props.isSetBlip()) {
                props.unsetBlip();
            }
        } else {
            props.setBlip(blip);
        }
    }

