    public void setTargetFrame(String frame) {
        if (frame == null) {
            if (link.isSetTgtFrame()) {
                link.unsetTgtFrame();
            }
        } else {
            link.setTgtFrame(frame);
        }
    }

