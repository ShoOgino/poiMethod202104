    public void setLanguage(Locale lang) {
        if (lang == null) {
            if (props.isSetLang()) {
                props.unsetLang();
            }
        } else {
            props.setLang(lang.toLanguageTag());
        }
    }

