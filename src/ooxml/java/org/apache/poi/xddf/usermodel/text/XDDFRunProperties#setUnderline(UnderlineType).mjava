    public void setUnderline(UnderlineType underline) {
        if (underline == null) {
            if (props.isSetU()) {
                props.unsetU();
            }
        } else {
            props.setU(underline.underlying);
        }
    }

