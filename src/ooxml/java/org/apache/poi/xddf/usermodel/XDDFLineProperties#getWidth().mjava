    /**
     * @return the width expressed in points.
     */
    public Double getWidth() {
        if (props.isSetW()) {
            return Units.toPoints(props.getW());
        } else {
            return null;
        }
    }

