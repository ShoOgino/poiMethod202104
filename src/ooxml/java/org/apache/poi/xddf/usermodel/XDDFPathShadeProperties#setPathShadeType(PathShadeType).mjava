    public void setPathShadeType(PathShadeType path) {
        if (path == null) {
            if (props.isSetPath()) {
                props.unsetPath();
            }
        } else {
            props.setPath(path.underlying);
        }
    }

