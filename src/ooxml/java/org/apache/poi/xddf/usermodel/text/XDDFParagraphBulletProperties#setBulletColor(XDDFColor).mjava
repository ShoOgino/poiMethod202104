    public void setBulletColor(XDDFColor color) {
        props.unsetBuClrTx();
        if (color == null) {
            props.unsetBuClr();
        } else {
            props.setBuClr(color.getColorContainer());
        }
    }

