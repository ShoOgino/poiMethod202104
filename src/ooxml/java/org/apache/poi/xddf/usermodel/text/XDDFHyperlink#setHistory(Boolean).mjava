    public void setHistory(Boolean history) {
        if (history == null) {
            if (link.isSetHistory()) {
                link.unsetHistory();
            }
        } else {
            link.setHistory(history);
        }
    }

