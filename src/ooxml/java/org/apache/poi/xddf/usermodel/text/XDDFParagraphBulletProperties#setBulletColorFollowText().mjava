    public void setBulletColorFollowText() {
        props.unsetBuClr();
        if (props.isSetBuClrTx()) {
            // nothing to do: already set
        } else {
            props.addNewBuClrTx();
        }
    }

