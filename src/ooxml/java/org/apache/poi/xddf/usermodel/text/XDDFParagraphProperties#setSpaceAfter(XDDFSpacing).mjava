    public void setSpaceAfter(XDDFSpacing spacing) {
        if (spacing == null) {
            props.unsetSpcAft();
        } else {
            props.setSpcAft(spacing.getXmlObject());
        }
    }

