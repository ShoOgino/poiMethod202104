    public void setLimit(Integer limit) {
        if (limit == null) {
            if (join.isSetLim()) {
                join.unsetLim();
            }
        } else {
            join.setLim(limit);
        }
    }

