    public void setPresetPattern(PresetPattern pattern) {
        if (pattern == null) {
            if (props.isSetPrst()) {
                props.unsetPrst();
            }
        } else {
            props.setPrst(pattern.underlying);
        }
    }

