    public Double getRightInset() {
        if (props.isSetRIns()) {
            return Units.toPoints(POIXMLUnits.parseLength(props.xgetRIns()));
        } else {
            return null;
        }
    }

