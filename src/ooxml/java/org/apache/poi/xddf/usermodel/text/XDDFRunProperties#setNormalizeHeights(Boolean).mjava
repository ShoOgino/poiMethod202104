    public void setNormalizeHeights(Boolean normalize) {
        if (normalize == null) {
            props.unsetNormalizeH();
        } else {
            props.setNormalizeH(normalize);
        }
    }

