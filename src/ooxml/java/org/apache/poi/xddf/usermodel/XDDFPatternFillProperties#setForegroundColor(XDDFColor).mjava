    public void setForegroundColor(XDDFColor color) {
        if (color == null) {
            if (props.isSetFgClr()) {
                props.unsetFgClr();
            }
        } else {
            props.setFgClr(color.getColorContainer());
        }
    }

