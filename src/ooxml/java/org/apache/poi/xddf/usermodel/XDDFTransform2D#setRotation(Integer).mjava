    public void setRotation(Integer rotation) {
        if (rotation == null) {
            if (transform.isSetRot()) {
                transform.unsetRot();
            }
        } else {
            transform.setRot(rotation);
        }
    }

