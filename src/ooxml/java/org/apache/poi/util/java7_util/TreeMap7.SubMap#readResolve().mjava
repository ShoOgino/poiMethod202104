        private Object readResolve() {
            return new AscendingSubMap(TreeMap7.this,
                                       fromStart, fromKey, true,
                                       toEnd, toKey, false);
        }

