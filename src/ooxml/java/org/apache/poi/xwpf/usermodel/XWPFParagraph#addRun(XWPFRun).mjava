    /**
     * adds a new Run to the Paragraph
     * 
     * @param r
     * @return
     */
    public void addRun(XWPFRun r) {
        if (!runs.contains(r)) {
            runs.add(r);
        }
    }

