    public void testReferencesOpr() {
        String[] operation = new String[] {
                            "+", "-", "*", "/", "^", "&"
                           };
        for (final String op : operation) {
            operationRefTest(op);
        }
    }

