	public void testUnicodeTextParagraphs() throws Exception {
		Range r = u.getRange();
		assertEquals(
				u_page_1 +
				page_break + "\r" + 
				u_page_2,
				r.text()
		);
		
		assertEquals(
				5,
				r.numParagraphs()
		);
		
		System.out.println(r.getParagraph(2).text());
	}

