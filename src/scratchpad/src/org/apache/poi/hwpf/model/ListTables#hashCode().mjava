    @Override
    public int hashCode()
    {
        final int prime = 31;
        int result = 1;
        result = prime * result
                + ( ( _listMap == null ) ? 0 : _listMap.hashCode() );
        result = prime * result
                + ( ( _plfLfo == null ) ? 0 : _plfLfo.hashCode() );
        return result;
    }

