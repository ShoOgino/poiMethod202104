    @Override
    public String toString()
    {
        return "Field [" + getStartOffset() + "; " + getEndOffset()
                + "] (type: 0x" + Integer.toHexString( getType() ) + " = "
                + getType() + " )";
    }

