    @Override
    public boolean equals( Object o )
    {
        if (!(o instanceof SEPX)) return false;
        SEPX sepx = (SEPX) o;
        if ( super.equals( o ) )
        {
            return sepx._sed.equals( _sed );
        }
        return false;
    }

