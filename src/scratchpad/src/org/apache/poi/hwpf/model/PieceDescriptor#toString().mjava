    @Override
    public String toString()
    {
        return "PieceDescriptor (pos: " + getFilePosition() + "; "
                + ( isUnicode() ? "unicode" : "non-unicode" ) + "; prm: "
                + getPrm() + ")";
    }

