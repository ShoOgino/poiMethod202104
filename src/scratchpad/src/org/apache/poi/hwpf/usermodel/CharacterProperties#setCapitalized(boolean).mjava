  public void setCapitalized(boolean caps)
  {
    if (caps != isFCaps())
    {
      byte newVal = (byte)(caps ? 1 : 0);
      _chpx.addSprm(SPRM_FCAPS, newVal);
      super.setFCaps(caps);
    }
  }

