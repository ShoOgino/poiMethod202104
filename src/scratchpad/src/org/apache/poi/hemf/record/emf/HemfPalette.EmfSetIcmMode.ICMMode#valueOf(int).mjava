            public static ICMMode valueOf(int id) {
                for (ICMMode wrt : values()) {
                    if (wrt.id == id) return wrt;
                }
                return null;
            }

