        public static HemfRegionMode valueOf(int flag) {
            for (HemfRegionMode rm : values()) {
                if (rm.flag == flag) return rm;
            }
            return null;
        }

