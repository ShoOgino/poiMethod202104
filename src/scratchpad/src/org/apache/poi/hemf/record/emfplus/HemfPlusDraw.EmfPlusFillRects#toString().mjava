        @Override
        public String toString() {
            return
                "{ flags: "+flags+
                ", brushId: "+brushId+
                ", rectData: "+rectData.stream().map(HwmfDraw::boundsToString).collect(joining(",", "{", "}"))+
                "}";
        }

