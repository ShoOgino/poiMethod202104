    public void draw(HemfPlusRecord r) {
        switch (renderState) {
            case EMFPLUS_ONLY:
            case EMF_DCONTEXT:
            case INITIAL:
                r.draw(this);
                break;
            case EMF_ONLY:
            default:
                break;
        }
    }

