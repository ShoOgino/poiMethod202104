    /**
     * Saves the current properties to the stack
     */
    public void saveProperties() {
        assert(prop != null);
        propStack.add(prop);
        prop = new HwmfDrawProperties(prop);
    }

