        @Override
        public void draw(HwmfGraphics ctx) {
            Area area = getShape(ctx);
            if (area == null) {
                return;
            }
            
            if (isFill()) {
                ctx.fill(area);
            } else {
                ctx.draw(area);
            }
        }

