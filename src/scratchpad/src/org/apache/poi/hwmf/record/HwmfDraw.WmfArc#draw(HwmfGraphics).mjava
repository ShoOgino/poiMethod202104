        @Override
        public void draw(HwmfGraphics ctx) {
            Shape s = getShape();
            switch (getWmfRecordType()) {
                default:
                case arc:
                    ctx.draw(s);
                    break;
                case chord:
                case pie:
                    ctx.fill(s);
                    break;
            }
        }

