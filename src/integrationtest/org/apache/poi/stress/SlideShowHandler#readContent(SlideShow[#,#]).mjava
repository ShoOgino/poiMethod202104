    private void readContent(SlideShow<?,?> ss) {
        for (Slide<?,?> s : ss.getSlides()) {
            s.getTitle();
            readText(s);
            readText(s.getNotes());
            readText(s.getMasterSheet());
        }
    }

