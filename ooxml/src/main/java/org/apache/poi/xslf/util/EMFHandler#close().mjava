    @Override
    public void close() throws IOException {
        if (is != null) {
            try {
                is.close();
            } finally {
                is = null;
            }
        }
    }

