    public void setLastColor(byte[] last) {
        if (last == null) {
            if (color.isSetLastClr()) {
                color.unsetLastClr();
            }
        } else {
            color.setLastClr(last);
        }
    }

