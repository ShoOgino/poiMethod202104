    public Double getTopInset() {
        if (props.isSetTIns()) {
            return Units.toPoints(POIXMLUnits.parseLength(props.xgetTIns()));
        } else {
            return null;
        }
    }

