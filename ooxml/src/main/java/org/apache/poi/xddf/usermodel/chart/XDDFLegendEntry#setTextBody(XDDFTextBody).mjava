    public void setTextBody(XDDFTextBody body) {
        if (body == null) {
            if (entry.isSetTxPr()) {
                entry.unsetTxPr();
            }
        } else {
            entry.setTxPr(body.getXmlObject());
        }
    }

