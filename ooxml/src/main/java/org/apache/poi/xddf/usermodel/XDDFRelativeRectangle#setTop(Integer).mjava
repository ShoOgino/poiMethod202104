    public void setTop(Integer top) {
        if (top == null) {
            if (rect.isSetT()) {
                rect.unsetT();
            }
        } else {
            rect.setT(top);
        }
    }

