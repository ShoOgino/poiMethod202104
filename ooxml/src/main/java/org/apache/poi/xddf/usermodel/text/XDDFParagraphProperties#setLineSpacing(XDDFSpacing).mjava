    public void setLineSpacing(XDDFSpacing spacing) {
        if (spacing == null) {
            if (props.isSetLnSpc()) {
                props.unsetLnSpc();
            }
        } else {
            props.setLnSpc(spacing.getXmlObject());
        }
    }

