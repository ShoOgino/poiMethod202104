    public void setPosition(Double position) {
        if (position == null) {
            if (stop.isSetPos()) {
                stop.unsetPos();
            }
        } else {
            stop.setPos(Units.toEMU(position));
        }
    }

