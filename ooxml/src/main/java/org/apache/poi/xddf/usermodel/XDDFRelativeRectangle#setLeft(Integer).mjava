    public void setLeft(Integer left) {
        if (left == null) {
            if (rect.isSetL()) {
                rect.unsetL();
            }
        } else {
            rect.setL(left);
        }
    }

